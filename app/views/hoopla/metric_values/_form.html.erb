<%= form_for(@hoopla_metric_value, url: form_path_of(@hoopla_metric_value)) do |f| %>
  <% if @hoopla_metric_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hoopla_metric_value.errors.count, "error") %> prohibited this hoopla_metric_value from being saved:</h2>

      <ul>
      <% @hoopla_metric_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id %>
  <%= f.hidden_field :metric_id %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
